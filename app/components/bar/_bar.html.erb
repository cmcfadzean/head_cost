<div class="system-bar">
  <%= link_to bar.cancel, class: "system-bar__cancel" do %>
    <i class="lni lni-close"></i>
  <% end %>
  <div class="system-bar__title"><%= bar.title %></div>
  <div class="system-bar__actions">
    <%= bar %>
  </div>
</div>